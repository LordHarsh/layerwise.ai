# Slim dependencies for Vercel serverless (250MB limit)
fastapi>=0.115.0
pydantic>=2.0.0
# Use OpenAI SDK (lighter) with Gemini's OpenAI-compatible endpoint
pydantic-ai-slim[openai]
python-multipart
sse-starlette
httpx
python-dotenv
# PDF processing (convert pages to images for Gemini vision)
pypdfium2
Pillow
